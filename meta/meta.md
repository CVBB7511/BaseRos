2026-06-19: 梳理了参考分支前端与 `se_map/se_navigation` 的依赖关系，按当前项目要求改为复用现有 `gazebosim_demo` 建图 launch 与 `mapping` 包，不新增额外建图/导航功能包。已迁移前端到 `frontend/`，移除导航与前端地图显示，改为设备启用、建图、保存地图、导入地图，并由 RViz 承担可视化。新增 `/frontend/*` ROS 服务控制节点和仿真验收脚本 `scripts/verify_sim_frontend_mapping.sh`，并通过 catkin 编译、前端 build 与服务导入检查。
2026-06-19: 按新需求移除前端“停止建图”按钮，改为“重新建图”，后端会舍弃未保存建图进度并重启建图 RViz。保存地图成功后后端会关闭建图 launch/RViz；导入地图后会启动 `palletizing_execute_real.launch map_file:=...` 供用户在 RViz 执行 `2D Pose Estimate`。前端新增桌面标定输入区，直接调用 `/palletizing/mark_zone` 保存取货桌或码垛桌参数，并通过 `pnpm build` 与 catkin 编译检查。
2026-06-19: 修复联调中保存地图误报失败的问题：保存后的进程清理改为 SIGINT/SIGTERM/SIGKILL 逐级尽力关闭，不再让关闭超时覆盖 `map_saver` 的成功结果。修复仿真导入地图误启真机 Kinect 的问题：`MapFile` 请求新增 `sim` 字段，前端按当前模式传递，后端仿真模式启动 `palletizing_execute.launch`，真机模式才启动 `palletizing_execute_real.launch`。同时给仿真执行 launch 增加 `map_file` 参数，已通过 catkin 编译、前端 build、服务字段和 Python 语法检查。
2026-06-19: 调整仿真联调架构，让 Gazebo 由新增的 `palletizing_sim_world.launch` 常驻单独启动，建图与执行 launch 增加 `start_gazebo/start_robot` 参数以复用已有仿真窗口，避免重复打开 Gazebo。前端优化了右侧面板与日志窗口，标定字段改为清晰中文标签并按仿真模型提供取货桌/码垛桌默认值。新增“开始码垛”按钮，后端 `/frontend/start_palletizing` 会调用现有 `/palletizing/start`，并通过前端 build、catkin 编译、launch 参数解析和 Python 语法检查。
2026-06-19: 梳理 `mark_table_positions.py` 与 `palletizing_executor.py`，新增 `docs/table_calibration_and_palletizing.md`，说明第 11 步桌面标定流程、`zones.yaml` 保存位置，以及第 12 步码垛执行流程，并在 `docs/pipeline.md` 中引用该文档。前端标定输入改为与 `rosrun palletizing mark_table_positions.py` 交互模式一致，只需区域、桌子长宽高、机器人到桌面中心距离；新增 `mapping/CalibrateTable` 服务，由后端读取 TF 自动计算桌面中心/yaw 后调用 `/palletizing/mark_zone` 保存。已通过 catkin 编译、前端 build、新服务字段检查和 Python 语法检查。
2026-06-19: 排查桌面标定未修改根目录 `zones.yaml` 的原因，发现仿真执行 launch 默认使用 `gazebosim_demo/config/palletizing_zones_sim.yaml`，已改为前端启动执行系统时显式传入 `/home/yubowen/BaseRos/zones.yaml`，并同步调整仿真/真机执行 launch 的 `zones_file` 默认值。根据当前 `palletizing_test.world` 和 table 模型给出仿真输入：桌子长度 1.2、宽度/深度 0.5、高度 0.765、距离 0.95，并更新前端默认值和文档说明。已通过临时 ROS master、静态 TF、`/frontend/calibrate_table` 实测写入 source/dest 到 `zones.yaml`，并通过前端 build 与后端语法检查。
2026-06-19: 按要求回退上一轮保存路径改动：仿真执行系统恢复默认使用 `catkin_ws/src/gazebosim_demo/config/palletizing_zones_sim.yaml`，后端启动执行 launch 时不再强行传入根目录 `zones.yaml`，真机执行 launch 也恢复为不显式覆盖 `zones_file`。补充 `docs/table_calibration_and_palletizing.md`，详细说明 `palletizing_zones_sim.yaml` 中 `source_*`、`dest_*` 各字段含义、前端标定输入含义，以及仿真推荐输入值：长度 1.20、宽度/深度 0.50、高度 0.765、距离 0.95。已通过仿真/真机执行 launch 的 `roslaunch --nodes` 解析检查和后端 Python 语法检查。
2026-06-19: 排查码垛启动后长时间不动的问题，判断终端中的 Fuel 连接超时来自 Gazebo 在线模型源查询，已新增离线 Fuel 配置并在仿真建图、仿真执行、常驻 Gazebo launch 以及仿真联调脚本中显式关闭在线 Fuel/模型库访问。为码垛过程新增 `/palletizing/stats` 周期发布和前端订阅展示，前端现在能显示当前状态、成功/失败、耗时和成功率，并在文档中说明详细日志位于“Frontend Control Services”终端及 `.ros/log`。新增真实场景前端联调脚本 `scripts/verify_real_frontend_palletizing.sh`，并通过 Python 语法、前端 build、脚本语法、仿真/真机 launch 节点解析检查。
2026-06-19: 优化前端右侧控制面板的分段按钮布局，让“仿真/真机”和“取货桌/码垛桌”在固定宽度面板中稳定均分显示，避免文字和图标被遮挡。按要求精简“码垛任务”状态卡片，仅保留当前状态和耗时，不再显示成功/失败与成功率。新增 `docs/frontend_integration_summary.md` 总结前端嵌入范围、ROS 对接、仿真/真机联调方式、日志位置，并说明摄像头画面可以通过压缩图像话题接入前端。
2026-06-19: 新增前端摄像头实时显示功能，连接 ROS Bridge 后自动订阅 `/kinect2/qhd/image_color_rect/compressed`，也可在界面中修改压缩图像话题和显示帧率。为保证流畅和不卡顿，前端只订阅 `sensor_msgs/CompressedImage`，默认 8 fps、最高 15 fps、订阅队列为 1，并直接更新 `<img>` 而不把大图像数据写入全局状态。同步更新 `docs/frontend_integration_summary.md`，说明摄像头显示的默认话题、性能策略和 ROS 侧压缩图像要求。
2026-06-19: 回答摄像头帧率问题：当前前端显示最高 15 fps 是前端保护上限，不是硬件固定值；实际可显示帧率还受相机发布频率、压缩开销、rosbridge、网络和浏览器解码能力影响。现有界面可在 1-15 fps 内调节，默认值可通过 `VITE_DEFAULT_CAMERA_FPS` 修改；若需要超过 15 fps，需要调整 `CameraPanel.vue` 中的上限并重新构建前端。
2026-06-19: 进一步确认相机发布帧率与前端显示帧率的关系：项目里的 `kinect2_bridge.launch` 存在 `fps_limit` 参数且默认 `-1.0`，表示 ROS 侧桥接节点不做软件限帧，真实发布频率由 Kinect 硬件、驱动、压缩、点云处理、CPU/USB 和订阅者负载共同决定，可能波动。前端当前只限制“显示/订阅后的绘制频率”，上限为 `CameraPanel.vue` 中的 15 fps；如果相机发布低于该值，前端只能跟随较低频率，如果发布高于该值，前端会丢弃旧帧只显示最新帧。
2026-06-19: 新增根目录 `.gitignore`，覆盖 ROS/catkin 生成目录、运行日志、前端构建产物、Python 缓存、压缩包、备份目录和临时地图，避免后续联调产物继续污染 Git 状态。确认忽略规则对 `.ros/`、`backup/`、`test_map/`、`catkin_ws.zip`、`frontend/dist`、`frontend/node_modules` 和新的 `real_maps/map-*` 文件生效；同时指出已被 Git 跟踪的 `catkin_ws/build`、`catkin_ws/devel` 需要用 `git rm --cached` 额外清理索引。给出推荐的 `git add`、commit 和 push 指令，避免直接 `git add .` 把构建产物或本地标定数据误提交。
2026-06-21: 核对后端最新码垛代码，确认当前默认执行器是简化版 `palletizing_executor.py`，并结合 Kinect 点云节点 `wpb_home_objects_3d` 与 `ObjectDetectionMixin` 完成单帧几何分类、多帧融合、风险排序和抓取目标选择，不是基于前端 RGB 画面的神经网络识别。实际 `run()` 与 `简化流程.txt` 主线一致，但后退距离按桌边净空动态计算、真机安全抬臂高度由 launch 覆盖为 0.90m，抓取/放置通过 topic 和结果反馈串行衔接，因此文本中的固定 0.5m、统一 0.8m只是概要。
2026-06-21: 两个前端联调脚本都会先执行 `catkin_make`，再由前端控制服务在导入地图时分别拉起最新的 `palletizing_execute.launch` 或 `palletizing_execute_real.launch`；两个 launch 均启动 `palletizing_executor.py`，catkin devel relay 也直接读取当前源码，所以会使用最新简化流程。需要注意联调脚本本身只启动基础环境，必须在前端完成“导入地图”并点击“开始码垛”后，最新执行流程才真正运行。
2026-06-21: 梳理了 RViz 方块识别框链路：C++ 点云节点先做平面分割、欧式聚类和尺寸筛选，生成单帧三维包围框与物体坐标；Python 检测模块再进行跨帧关联、命中次数过滤、中位数融合和类别投票，发布稳定的 `/palletizing/fused_marker`。当前 RViz 默认关闭绿色单帧 `/obj_marker`、启用青色融合框；该框是 `base_footprint` 坐标系中的三维 Marker，并非直接绘制到 RGB 图像像素上。
2026-06-21: 分析仿真剩余一个物体却提前终止的问题，确认该聚类尺寸 `(0.11, 0.15, 0.12)` 可通过 15cm 方块尺寸判定，但 `yMax=0.53` 超过 C++ 检测区域硬编码上限 `0.50m`，因此每帧均以 `reject_obj_0` 丢弃。两次检测重试都得到 0 个非空样本后，执行器将检测失败直接解释为取货桌已空并结束任务，故日志显示 `success=3 failed=0`；这是检测区域边界与终止判据共同导致的误结束。
2026-06-21: 新增前端底盘遥控组件，连接 ROS Bridge 并主动启用后，可通过 `W/S/A/D/Q/E` 或界面方向按钮按档调节 `/cmd_vel`，通过 `Space` 或红色停止按钮急停，并实时显示三轴速度。遥控以 10Hz 持续发布，窗口失焦、页面隐藏、关闭控制或组件卸载时自动发送零速度；输入框编辑不会触发遥控按键。仿真和真机联调脚本已移除独立键盘终端，相关文档同步更新，并通过前端生产构建和脚本语法检查。
2026-06-21: 将底盘遥控卡片移至页面左下角，并与码垛活动状态联动：任务开始后自动归零并锁定所有遥控按键和按钮，任务结束或人为终止后恢复。新增“终止码垛”按钮及 `/frontend/stop_palletizing`、`/palletizing/stop` 服务链路，终止时取消导航、停止检测/抓取/放置、发布零速度，执行器以 `ABORTED` 状态退出，前端显示“人为终止”。增加旧任务线程退出保护和状态保留发布，已通过前端构建、Python 语法、catkin 全量编译及差异检查。
2026-06-22: 修复码垛终止后耗时继续增长的问题，执行器现在记录任务结束时间，并在 `DONE`/`ABORTED` 后持续发布冻结的耗时；启动服务提示改为“开始码垛”。前端底栏不再重复展示操作结果，失败消息只进入一次错误日志，右侧日志支持清空、滚动和长路径换行。重新建图时若当前建图仍在运行，会弹出未保存进度警告，由用户选择确定或取消；已通过前端生产构建、Python 语法和差异检查。
2026-06-22: 将右侧执行日志改为带时间的持久化历史列表，每条成功或错误操作保存 ISO 时间、类型和正文到浏览器 `localStorage`，最多保留最近 100 条，刷新页面或重启同一地址的前端后仍可恢复。日志按最新优先显示，采用固定宽度约束、隐藏横向溢出和任意字符换行，长文件路径不会超出框体，同时保留手动清空功能；已通过前端生产构建和差异检查。
2026-06-22: 按新的显示设想将执行日志重构为独立卡片列表，每张卡片包含成功/失败图标、类型、时间和完整正文，并以不同左侧强调色区分结果。日志框固定可视高度、内部独立纵向滚动，卡片正文可在任意字符处换行并随内容增高，避免长路径或长文本越出框体；已通过前端生产构建和差异检查。
2026-06-22: 说明当前执行日志没有保存为项目目录中的普通文件，而是以键 `frontend_operation_logs` 存入浏览器针对前端源地址的 `localStorage`。日志可在浏览器开发者工具的 Application/Storage 页面查看；底层由浏览器配置目录中的 Local Storage/LevelDB 管理，不适合直接当作单一文本文件读取。
2026-06-22: 增加码垛正常完成日志，前端在活动任务首次从运行状态进入 `DONE` 时自动追加“码垛完成，耗时 N 秒”的成功卡片，并沿用现有时间戳和 `localStorage` 持久化机制。通过活动状态边沿判断避免执行器周期性重复发布 `DONE` 时重复记录，也避免前端重启订阅到旧的终态后再次写入；已通过前端生产构建和差异检查。
2026-06-22: 新增 `mapping/OperationLog` 服务和 `/frontend/operation_logs` 接口，将前端操作日志以 JSON Lines 格式持久化到项目文件 `logs/frontend_operations.log`，支持追加、读取、首次导入旧浏览器记录和清空。前端连接后以项目文件恢复最近 100 条卡片，`localStorage` 仅保留为服务离线时的副本；清空按钮会同步清除两处记录。已通过 catkin 全量编译、前端生产构建、Python/脚本检查，并在隔离 ROS 环境中实测追加、读取和清空成功，测试数据已清理。
